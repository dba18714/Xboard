services:
  web:
    build: .
    volumes:
      - app:/www/app
    ports:
      - "8080:7001"
    environment:
      - ENABLE_WEB=true
      - ENABLE_HORIZON=false
      - ENABLE_REDIS=false
      - APP_ENV=local
      - APP_DEBUG=true
    restart: unless-stopped
